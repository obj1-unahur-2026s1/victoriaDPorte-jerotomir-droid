import elementos.*
import deportes.*
import example.*

describe "group of tests for pepita" {

  test "parecerse a pareto" {
    victoria.definirAltura(150)
    victoria.disciplinaQuePractica(judo)
    victoria.definirCantidadEntrenadores(4)
    assert.equals(traje,victoria.elementoIndeispensable())
  }

}